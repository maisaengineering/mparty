<div class="container"><br>
<div id="calendar"></div>
</div>
<script>
    $('#calendar').fullCalendar({
    	header: {
				left: 'prev,next today',
				center: 'title',
				right: 'month,agendaWeek,agendaDay'
			},
			eventLimit: true,
      events: '/get_my_calendar.json',
      eventRender: function(event, element) {
        element.attr('title', event.tooltip);
    }
    });  
</script>