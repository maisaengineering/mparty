$('#comments').append('<%= j(render 'comments/list',comments: @comments )   %>');
<% if @comments.next_page %>
$('.pagination').replaceWith('<%= j(paginate @comments) %>');
<% else %>
$('.pagination').remove();
<% end %>

