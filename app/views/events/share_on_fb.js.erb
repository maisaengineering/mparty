var data=
{
    method: 'feed',
    message: "Hi I create a event in mparty",
    display: 'iframe',
    caption: "Mparty: Celebrate your Event with Heart Likes Gifts",
    name: "<%= @event.name %>",
    picture: "<%= @image_path %>",
    link: "<%= event_url(@event) %>",
    description: "abcd",
    actions: [{ name: 'Mparty', link: 'http://mparty.herokuapp.com/' }]
}
FB.ui(data, function (response){
    if (response && response.post_id) {
        bootbox.alert('Event successfully published on your FB timeline.');
    } else {
       // bootbox.alert('Oops you cancelled');
    }
});