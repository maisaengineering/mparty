<%= form_tag(send_invitation_path, class: 'form_margin',id: 'invite_friends_form') do %>
    <%= hidden_field_tag(:event_id, @event.id) %>
    <%= hidden_field_tag(:friend_emails) %>
<!--     <div class="col-md-4">
      <label></label>
      <a href="<%#= spree.root_url%>contacts/gmail" class="btn btn-lg btn-danger btn-block">GMail</a>
    </div> -->
    <div class="col-md-12">
     <!--  <a href="event_preview_v2.html" class="btn btn-primary pull-right btn-sm mright10" type="submit">EVENT PREVIEW</a> -->
      <button id="ImportContactsBtn" class="btn pull-right btn-primary btn-sm mright5" data-toggle="popover" data-placement="top"> IMPORT CONTACTS</button>
    </div>
    <div class="col-md-12 invitation_text">
      <label></label>
      <div id="tags" class="tags_wrap">
        <input type="text" name="emails_autocomplete" placeholder="Enter email with comma separated" class="biginput" id="emails_autocomplete">
      </div>
    </div>
    <div class="col-md-12"> 
      <ul id="import_contact_list">
      <% unless @contacts.blank? %>
        <% @contacts.each do |contact| %>
        <li>
          <%= check_box("contacts", contact[:email],{checked: false}) %> <%=contact[:name]%> => <%=contact[:email]%>
          <br>
           </li>
        <% end %>
      <% end %>
      </ul>
    </div>
    <div class="col-md-4">
      <label></label>
      <a href="<%= event_url(@event) %>" class="btn btn-lg btn-primary btn-block invite_btn_cancel" type="submit">CANCEL</a> </div>
    <div class="col-md-4">
      <label></label>
      <%= submit_tag("INVITE",class: 'btn btn-lg btn-primary btn-block upload_btn_done') %>
    </div>
<% end %>




<script type="text/javascript">
    function validateEmail(txtEmail){
        var a = txtEmail
        var filter = new RegExp(/^(("[\w-\s]+")|([\w-]+(?:\.[\w-]+)*)|("[\w-\s]+")([\w-]+(?:\.[\w-]+)*))(@((?:[\w-]+\.)*\w[\w-]{0,66})\.([a-z]{2,6}(?:\.[a-z]{2})?)$)|(@\[?((25[0-5]\.|2[0-4][0-9]\.|1[0-9]{2}\.|[0-9]{1,2}\.))((25[0-5]|2[0-4][0-9]|1[0-9]{2}|[0-9]{1,2})\.){2}(25[0-5]|2[0-4][0-9]|1[0-9]{2}|[0-9]{1,2})\]?$)/i);
        if(filter.test(a)){
            return true;
        }
        else{
            return false;
        }
    }

  $('#invite_friends_form  #tags input').on('focusout',function(){
    var txt= this.value.replace(/^[\s]+|,|("[\s]+")$/g,''); // // space need to remove at the end allowed characters
    if(txt) {
        if(validateEmail(txt)){
            $(this).before('<span class="tag">'+ txt.toLowerCase() +'</span>');
            this.value="";
        }else{
            bootbox.alert(txt+' is not valid email')
            this.value="";
        }
    }
  }).on('keyup',function( e ){
      e.stopPropagation()
      // space 32
    if(/(188|13)/.test(e.which)) $(this).focusout(); // if: comma,enter (delimit more keyCodes with | pipe)
     // alert(e.which)
  });
  
  
  $('#invite_friends_form #tags').on('click','.tag',function(){
     if(confirm("Do you want to remove invitee's email address?")) $(this).remove();
  });
  
  $('#invite_friends_form  .tags_wrap').click(function() {
         $('#tags input').focus();
  })

  $("#invite_friends_form").submit(function(e) {  
    emails = []
    $('#tags').children('.tag').each(function () {
     emails.push($(this).text()); // "this" is the current element in the loop
    });
    $('#friend_emails').val(emails.join()) 
  })

var temp = '<a class="btn btn-default btn-block" href="<%= spree.root_url%>contacts/gmail"><img src="/assets/import/logo_gmail.png" width="100" height="35" alt=""/></a>';

$("#ImportContactsBtn").click(function (e) {
  e.preventDefault();
})

$("#ImportContactsBtn").popover({  
    animation:true,
    content: temp,
    html:true,
    trigger:'hide',
    container:'body'
  });
</script>

<style type="text/css">
#tags input{
  background:#eee;
  border:0;
  margin:4px;
  padding:7px;
  width:auto;
  background-color:transparent;
  outline:none;
  /*dinesh to increase text length for placeholder in events/_invite_form.html */
  width: 270px;
}
</style>